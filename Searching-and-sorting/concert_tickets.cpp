#include <bits/stdc++.h>

using namespace std;

vector<int> read_vector(int n) {
    vector<int> vec(n);
    for (int i = 0; i < n; i++) {
        cin >> vec[i];
    }
    return vec;
}

int main() {
    int n,m;
    cin>>n>>m;
    
    return 0;
}